# API base URL (no trailing slash). For local dev use http://localhost:5000
# On Vercel, set this in Project Settings → Environment Variables to your Render backend URL.
VITE_API_URL=http://localhost:5000
